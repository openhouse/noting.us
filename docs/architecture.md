# Architecture

The scaffold is a TypeScript pnpm monorepo with separate packages for domain types, ingestion, fixture generation, wiki preview, and evals. The Next.js web app imports deterministic fixtures directly so the demo runs with no services.
