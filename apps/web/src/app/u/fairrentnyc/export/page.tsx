import {Nav} from '../../../../components/Nav'; import {bundles} from '../../../../lib/demo'; export default function Page(){const b=bundles[0]; return <><Nav/><h1>Markdown Export</h1><p>Export is a folder/zip seam preserving Markdown and sidecars.</p><h2>Included files</h2>{['page.md','page.meta.json','sources.jsonl','evals.json','permissions.json','wiki-export.wikitext'].map(f=><span className="badge" key={f}>{f}</span>)}<h2>Preview: {b.wikiTitle}</h2><pre>{b.markdown}</pre><pre>{JSON.stringify(b.metadata,null,2)}</pre></>}
