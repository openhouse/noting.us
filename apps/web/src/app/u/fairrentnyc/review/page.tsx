import {Nav} from '../../../../components/Nav'; import {bundles} from '../../../../lib/demo'; export default function Page(){const b=bundles.find(x=>x.slug==='legal-bill-text-review')!; return <><Nav/><h1>Human Review Queue</h1><div className="card warn"><h2>{b.wikiTitle}</h2><p>Demo affordances: Approve · Request changes · Mark disputed · Reject · Make correction.</p><h3>Bot draft</h3><pre>{b.markdown.slice(0,900)}</pre><h3>Human correction</h3><pre>{b.markdown.replace('Several provisions need legal / policy review before becoming settled campaign position.','Several provisions are flagged as needing legal / policy review as of the 2026-05-29 source date; they are not settled campaign position in this demo.').slice(0,900)}</pre>{b.evals.map(e=><span className="badge" key={e.id}>{e.name}: {e.status}</span>)}</div></>}
